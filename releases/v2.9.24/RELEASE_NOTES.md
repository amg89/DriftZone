# Drift Zone v2.9.24

## What's in this release (2025-02-26)

- **NEW: "Hide out of stock" toggle** added to the Inventory tab, next to Search and Sort — matches the same toggle already in the Snacks Order menu.
- Correctly accounts for recipe-based items (uses "can make" quantity, not just the raw stock field) — consistent with how availability is judged everywhere else in the app.

## Upload instructions

1. Create `releases/v2.9.24/` in your GitHub repo
2. Add `drift-zone.html` into that folder
3. Commit: `Release v2.9.24 — add Hide Out of Stock toggle to Inventory tab`
